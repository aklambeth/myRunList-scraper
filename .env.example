# North Hampshire Hash House Harriers
# gid of the published Google Sheet tab
NH4_API_KEY=

# Deepcut Hash House Harriers
# Fouita widget UID
DH3_API_KEY=

# Worthy Winchester Hash House Harriers — Google Maps embed only (no coordinates at source)
# Shared enrichment key for Google Geocoding API (optional).
# When unset (or unavailable), the geocode step falls back to the keyless
# Nominatim / OpenStreetMap geocoder instead of skipping.
GOOGLE_GEOCODING_API_KEY=

# Guildford Hash House Harriers — no key required (server-rendered HTML)
# R2D2 Hash House Harriers — no key required (public ASPX endpoint)
